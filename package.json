{
  "name": "root",
  "private": true,
  "scripts": {
    "do-publish": "lerna run build && lerna publish"
  },
  "devDependencies": {
    "@types/node": "^14.0.13",
    "lerna": "^3.22.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
